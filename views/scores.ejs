<!DOCTYPE html>
<html lang = 'eng'>
  <head>
  </head>
  <body class = "container">
    <section id = "mainSection">
        High scores!:
      <ul>
      <% db.forEach(function(score){ %>

        <li> <%= score.username %> : <%= score.points %>

          <% }); %>
        </ul>

    </section>
  </body>
</html>
